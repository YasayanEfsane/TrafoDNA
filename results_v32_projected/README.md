# V3.2 projected-PUF development outputs

Run `run_v32_projected_puf_development` to generate the exploratory MAT and CSV artifacts. Generated outputs are excluded from source archives.

The study uses enrollment, validation, known-test, and unseen-development rows only. Locked V3 final rows are excluded, so these files cannot be presented as a new final-holdout result.
