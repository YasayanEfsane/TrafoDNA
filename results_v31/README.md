# Generated V3.1 capacity-audit results

This directory contains only a tracked placeholder. Running `analyzeV31Capacity` writes the post-V3 development MAT bundle and CSV diagnostics here. Generated outputs are ignored by Git.

The audit does not use locked V3 final rows for selection or development metrics and does not change the V3 `NOT SUPPORTED` decision.

The first default audit is documented in `docs/V31_CAPACITY_AUDIT.md`: exhaustive graph search proved a maximum of 26 admissible bits from 179 eligible candidates, below the target of 32.
